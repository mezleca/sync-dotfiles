{
  "name": "sync-dots",
  "module": "src/index.ts",
  "type": "module",
  "private": false,
  "scripts": {
    "build": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile build/sync-dots"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.8.6",
    "inquirer": "^12.9.6"
  }
}
